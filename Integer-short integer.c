#include <stdio.h>


int main()
{
    // We can use here short int or short only all are worked!.
    short int distance=34;
    //We can use here long int or only long all data types worked!.
    long int
     population=7000000;
    printf("%d\n",distance);
    printf("%u",population);

    return 0;
}
